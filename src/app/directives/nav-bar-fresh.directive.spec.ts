import { NavBarFreshDirective } from './nav-bar-fresh.directive';

describe('NavBarFreshDirective', () => {
  it('should create an instance', () => {
    const directive = new NavBarFreshDirective();
    expect(directive).toBeTruthy();
  });
});
